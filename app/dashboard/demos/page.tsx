import { PhasePlaceholder } from "@/components/dashboard/PhasePlaceholder";

export default function DemosPage() {
  return (
    <PhasePlaceholder
      title="Demos"
      phase="Fase 9"
      description="Generación de demos de nueva web usando únicamente datos reales del negocio (nombre, dirección, teléfono, servicios) — nunca se publican automáticamente sin tu confirmación."
    />
  );
}
